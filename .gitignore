# Capacitor staging dir — created by `mkdir -p www && cp index.html www/`
www/

# Capacitor native projects (generated by `npx cap add ios|android`)
ios/
android/

# node
node_modules/
package-lock.json
